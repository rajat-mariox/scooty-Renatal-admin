# Copy to .env.local to override the backend on this machine only (git-ignored).
# Vercel: set VITE_API_BASE_URL under Project Settings > Environment Variables.
VITE_API_BASE_URL=https://backend.slydomobility.com/v1/api
